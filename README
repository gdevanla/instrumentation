To install into mvn repo:
    mvn install

To use as java agent

    java -javaagent:instrumentation-0.0.1-SNAPSHOT.jar <application class to instrument>


Other Notes:

    Currently, the application only print limited method attributes. All the info is printed in Json format using a light weight Json formatter. TODOs: make the data generated by instrumentor configurable.
